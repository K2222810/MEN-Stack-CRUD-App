<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Pizza</title>
    <link rel="stylesheet" href="/styles.css">
    <script src="/images.js"></script>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="page-container">
      <div class="page-header">
        <h1>Edit Your Pizza</h1>
        <p>Update pizza details</p>
      </div>
      
      <form action="/recipes/<%= recipe._id %>?_method=PUT" method="POST">
        <div class="form-group">
          <label for="pizzaType">Pizza Type:</label>
          <input type="text" id="pizzaType" name="pizzaType" value="<%= recipe.pizzaType %>" required />
        </div>
        
        <div class="form-group">
          <label for="imageSelect">Select Pizza Image:</label>
          <select id="imageSelect">
            <option value="">Choose an image...</option>
            <option value="/assets/margherita.png" <% if (recipe.image === '/assets/margherita.png') { %>selected<% } %>>Margherita</option>
            <option value="/assets/pepperoni.png" <% if (recipe.image === '/assets/pepperoni.png') { %>selected<% } %>>Pepperoni</option>
            <option value="/assets/vegetarian.png" <% if (recipe.image === '/assets/vegetarian.png') { %>selected<% } %>>Vegetarian</option>
            <option value="/assets/hawaiian.png" <% if (recipe.image === '/assets/hawaiian.png') { %>selected<% } %>>Hawaiian</option>
            <option value="/assets/bbq-chicken.png" <% if (recipe.image === '/assets/bbq-chicken.png') { %>selected<% } %>>BBQ Chicken</option>
            <option value="/assets/four-cheese.png" <% if (recipe.image === '/assets/four-cheese.png') { %>selected<% } %>>Four Cheese</option>
            <option value="/assets/meat-lovers.png" <% if (recipe.image === '/assets/meat-lovers.png') { %>selected<% } %>>Meat Lovers</option>
            <option value="/assets/seafood.png" <% if (recipe.image === '/assets/seafood.png') { %>selected<% } %>>Seafood</option>
          </select>
          <input type="hidden" id="image" name="image" value="<%= recipe.image || '' %>" />
          <div id="imagePreview" style="margin-top: 15px;">
            <% if (recipe.image) { %>
              <img src="<%= recipe.image %>" alt="<%= recipe.pizzaType %>" style="max-width: 200px; border-radius: 5px;" />
            <% } %>
          </div>
        </div>
        
        <div class="form-group">
          <label for="size">Size:</label>
          <select id="size" name="size" required>
            <option value="small" <% if (recipe.size === 'small') { %>selected<% } %>>Small</option>
            <option value="medium" <% if (recipe.size === 'medium') { %>selected<% } %>>Medium</option>
            <option value="large" <% if (recipe.size === 'large') { %>selected<% } %>>Large</option>
          </select>
        </div>
        
        <div class="form-group">
          <label for="crust">Crust:</label>
          <input type="text" id="crust" name="crust" value="<%= recipe.crust %>" required />
        </div>
        
        <div class="form-group">
          <label for="sauce">Sauce:</label>
          <input type="text" id="sauce" name="sauce" value="<%= recipe.sauce %>" required />
        </div>
        
        <div class="form-group">
          <label for="cheeseLevel">Cheese Level:</label>
          <input type="text" id="cheeseLevel" name="cheeseLevel" value="<%= recipe.cheeseLevel %>" />
        </div>
        
        <div class="form-group">
          <label for="instructions">Instructions:</label>
          <textarea id="instructions" name="instructions"><%= recipe.instructions %></textarea>
        </div>
        
        <div class="form-group">
          <label for="ingredients">Select Ingredients:</label>
          <select id="ingredients" name="ingredients" multiple>
            <% ingredients.forEach(ingredient => { %>
              <% const isSelected = recipe.ingredients.some(selected => selected._id.toString() === ingredient._id.toString()); %>
              <option value="<%= ingredient._id %>" <% if (isSelected) { %>selected<% } %>><%= ingredient.ingredients %></option>
            <% }); %>
          </select>
          <div class="info-text">
            <small>Hold Ctrl (or Cmd on Mac) to select multiple ingredients</small>
          </div>
        </div>
        
        <button type="submit">Update Pizza</button>
      </form>
      
      <div class="btn-group" style="max-width: 600px; margin-left: auto; margin-right: auto;">
        <a href="/recipes/<%= recipe._id %>" class="btn-link btn-secondary">View Pizza</a>
        <a href="/recipes" class="btn-link btn-secondary">Back to My Pizzas</a>
      </div>
    </div>
  </body>
</html>
