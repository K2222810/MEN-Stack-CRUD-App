<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Pizza</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit Pizza</h1>
    
    <form action="/recipes/<%= recipe._id %>?_method=PUT" method="POST">
      <label for="pizzaType">Pizza Type:</label>
      <input type="text" id="pizzaType" name="pizzaType" value="<%= recipe.pizzaType %>" required />
      
      <label for="size">Size:</label>
      <select id="size" name="size" required>
        <option value="small" <% if (recipe.size === 'small') { %>selected<% } %>>Small</option>
        <option value="medium" <% if (recipe.size === 'medium') { %>selected<% } %>>Medium</option>
        <option value="large" <% if (recipe.size === 'large') { %>selected<% } %>>Large</option>
      </select>
      
      <label for="crust">Crust:</label>
      <input type="text" id="crust" name="crust" value="<%= recipe.crust %>" required />
      
      <label for="sauce">Sauce:</label>
      <input type="text" id="sauce" name="sauce" value="<%= recipe.sauce %>" required />
      
      <label for="cheeseLevel">Cheese Level:</label>
      <input type="text" id="cheeseLevel" name="cheeseLevel" value="<%= recipe.cheeseLevel %>" />
      
      <label for="instructions">Instructions:</label>
      <textarea id="instructions" name="instructions"><%= recipe.instructions %></textarea>
      
      <label for="ingredients">Select Ingredients:</label>
      <select id="ingredients" name="ingredients" multiple>
        <% ingredients.forEach(ingredient => { %>
          <% const isSelected = recipe.ingredients.some(selected => selected._id.toString() === ingredient._id.toString()); %>
          <option value="<%= ingredient._id %>" <% if (isSelected) { %>selected<% } %>><%= ingredient.ingredients %></option>
        <% }); %>
      </select>
      <p><small>Hold Ctrl (or Cmd on Mac) to select multiple ingredients</small></p>
      
      <button type="submit">Update Pizza</button>
    </form>
    
    <a href="/recipes/<%= recipe._id %>">View Pizza</a>
    <a href="/recipes">Back to My Pizzas</a>
  </body>
</html>
