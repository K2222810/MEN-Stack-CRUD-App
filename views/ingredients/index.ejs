<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pizza Cap - Ingredients Index</title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="page-container">
      <div class="page-header">
        <p>Create and manage your ingredient catalogue</p>
      </div>
      
      <div class="ingredient-form">
        <h2>Add New Ingredient</h2>
        <form action="/ingredients" method="POST">
          <div class="form-group">
            <label for="ingredients">Ingredient Name:</label>
            <input type="text" id="ingredients" name="ingredients" placeholder="e.g., Mozzarella, Basil, Tomato" required />
          </div>
          <div class="form-group">
            <label for="category">Category:</label>
            <select id="category" name="category" required>
              <option value="">Select Category</option>
              <option value="meat">Meat</option>
              <option value="veggies">Vegetables</option>
              <option value="cheese">Cheese</option>
              <option value="sauce">Sauce</option>
            </select>
          </div>
          <button type="submit">Add Ingredient</button>
        </form>
      </div>
      
      <div class="item-list">
        <h2>All Ingredients</h2>
        <% if (ingredients.length > 0) { %>
          <ul>
            <% ingredients.forEach(ingredient => { %>
              <li>🥫 <%= ingredient.ingredients || ingredient.name %></li>
            <% }); %>
          </ul>
        <% } else { %>
          <div class="empty-message">
            <p>No ingredients yet. Add your first ingredient above!</p>
          </div>
        <% } %>
      </div>
      
      <div class="btn-group" style="max-width: 600px; margin-left: auto; margin-right: auto;">
        <a href="/recipes" class="btn-link btn-secondary">Back to My Recipes</a>
        <a href="/recipes/new" class="btn-link">Create Pizza</a>
      </div>
    </div>
  </body>
</html>
